import { describe, beforeAll, afterAll, test, expect } from "@jest/globals";

describe("Get All Items unit tests", () => {
  test("Normal Case", () => {
    // Dummy test for seeing if the setup works
    expect(1 + 1).toEqual(2);
  });
});
